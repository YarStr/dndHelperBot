package dataIO;

/**
 * Интерфейс модуля, принимающего сообщения от пользователя
 */
public interface InputModule {
     /**
      * Функция получения сообщения
      * @return возвращает сообщение
      */
     String getMessage();
}
